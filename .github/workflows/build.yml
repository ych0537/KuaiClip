name: Build & Test

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    name: Build KuaiClip
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show Swift version
        run: swift --version

      - name: Run tests
        run: bash scripts/test.sh

      - name: Build release
        run: swift build -c release

      - name: Package app, zip, and pkg
        run: BUILD_DIR=.build/release VERSION="${GITHUB_REF_NAME#v}" bash scripts/package.sh

      - name: Verify packages exist
        run: |
          ls -la release/KuaiClip.app/Contents/MacOS/KuaiClip
          file release/KuaiClip.app/Contents/MacOS/KuaiClip
          test -f release/KuaiClip.app.zip
          test -f release/KuaiClip.pkg

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: KuaiClip-unsigned-build
          path: |
            release/KuaiClip.app.zip
            release/KuaiClip.pkg
